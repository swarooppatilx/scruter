<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Exclusive Discount Offers</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f5f5f5;
            color: #333;
            text-align: center;
        }
        .discount-banner {
            background-color: #ff4757;
            color: white;
            padding: 50px 20px;
        }
        .discount-banner h1 {
            font-size: 3em;
            margin: 0;
        }
        .discount-banner p {
            font-size: 1.5em;
        }
        .offer-expiry {
            font-size: 1.2em;
            color: #ffeaa7;
        }
        .product-list {
            display: flex;
            justify-content: center;
            margin: 40px 0;
            flex-wrap: wrap;
        }
        .product-item {
            background-color: white;
            border: 1px solid #ddd;
            padding: 20px;
            margin: 10px;
            width: 250px;
            text-align: left;
            box-shadow: 0px 5px 15px rgba(0, 0, 0, 0.1);
            transition: transform 0.2s;
        }
        .product-item:hover {
            transform: scale(1.05);
        }
        .product-name {
            font-size: 1.2em;
            color: #333;
        }
        .product-price {
            font-size: 1.1em;
            text-decoration: line-through;
            color: #aaa;
        }
        .discounted-price {
            font-size: 1.5em;
            color: #ff4757;
        }
        .footer {
            margin-top: 40px;
            padding: 20px;
            background-color: #333;
            color: white;
        }
    </style>
</head>
<body>

    <!-- Discount Banner -->
    <div class="discount-banner">
        <h1>Get <%= discountPercentage %>% OFF!</h1>
        <p>Don't miss out on our exclusive offers.</p>
        <p class="offer-expiry">Offer valid until <%= offerExpiry %>.</p>
    </div>

    <!-- Product List -->
    <div class="product-list">
        <% products.forEach(function(product) { %>
            <div class="product-item">
                <h2 class="product-name"><%= product.name %></h2>
                <p class="product-price">$<%= product.price %></p>
                <p class="discounted-price">$<%= product.discountedPrice %></p>
            </div>
        <% }); %>
    </div>

    <!-- Footer -->
    <div class="footer">
        <p>Hurry up! Limited time only.</p>
    </div>

</body>
</html>
